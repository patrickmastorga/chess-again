[package]
name = "chess-again"
version = "0.1.0"
edition = "2024"

[lib]
name = "chess_again"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.29.0", optional = true, features = ["extension-module"] }

[features]
default = []
python = ["pyo3"]